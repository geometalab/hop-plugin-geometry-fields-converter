image: maven:latest

stages:
  - build
  - test

before_script:
  - mkdir -p target

cache:
  paths:
    - target/

#build:
#  stage: build
#  script:
#    - mvn clean package
#  artifacts:
#    paths:
#      - target/*.jar
#
#unit_tests:
#  stage: test
#  script:
#    - mvn test

integration_tests:
  stage: test
  image: 
    name: apache/hop:latest
    entrypoint: [""]
  script:
    - hop-run.bat -j samples -r local -f ${PROJECT_HOME}/transforms/switch-case-basic.hpl
